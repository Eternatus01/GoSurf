{
  "name": "npm",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cd dist && touch .nojekyll && git init && git add -A && git commit -m 'deploy' && git push -f https://github.com/Eternatus01/GoSurf.git main:gh-pages"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.3",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.3",
    "sass": "^1.86.3",
    "vite": "^6.2.4",
    "vite-plugin-vue-devtools": "^7.7.2"
  }
}
